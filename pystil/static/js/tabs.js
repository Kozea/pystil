// Generated by CoffeeScript 1.6.2
(function() {
  $.fn.extend({
    tabshow: function(fn) {
      return this.each(function(i, e) {
        var evt;

        if (fn) {
          return $.event.add(e, "tabshow", fn, null);
        } else {
          evt = $.Event("tabshow", {
            target: e
          });
          return $.event.trigger("tabshow", evt, e, false);
        }
      });
    }
  });

  $(function() {
    return $('.pystil-tabs').each(function(i, elt) {
      var $lnks, $tab;

      $lnks = $('ul li a', elt);
      $tab = $('.tab', elt);
      $lnks.first().addClass('active');
      $tab.first().addClass('active').tabshow().css({
        left: 0
      });
      $tab.not($tab.first()).hide().css({
        left: window.innerWidth
      });
      return $lnks.click(function(evt) {
        var $new_lnk_active, $new_tab_active, $old_lnk_active, $old_tab_active;

        $old_tab_active = $('.tab.active', elt);
        $new_tab_active = $("#" + (this.href.split('#')[1]));
        $old_lnk_active = $('ul li a.active', elt);
        $new_lnk_active = $(this);
        $old_tab_active.removeClass('active');
        $old_lnk_active.removeClass('active');
        $new_tab_active.addClass('active');
        $new_lnk_active.addClass('active');
        $new_tab_active.show();
        $tab.each(function(i, e) {
          var sign;

          sign = i - $tab.index($new_tab_active);
          return $(e).css({
            left: sign * window.innerWidth
          });
        });
        $new_tab_active.tabshow();
        return false;
      });
    });
  });

}).call(this);

/*
//@ sourceMappingURL=tabs.map
*/
